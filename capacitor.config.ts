import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pt.space.tech',
  appName: 'TechSpace',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
